# BurpSuite_Docker

# Clone the repository.

cd BurpSuite_Docker/

# Build Dockerfile.

docker build -t burpsuite .

# Docker compose run.

docker-compose up -d

# Verify Docker Process.

docker ps

# Burpsuite is up and accessible now.
